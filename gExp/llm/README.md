# gExp LLM Context

Read the root `AGENTS.md`, `gSetup/llm/README.md`, and `gExp/AGENTS.md` before
working in this group. Use the nearest child-module contract for implementation.

Generated source-derived context belongs under `generated/` and must not be
edited manually. Human-authored contracts and examples must preserve the
backend/API-only experience boundary and later-module override model.
