# kickoffProd

`kickoffProd` is the production environment module for the `kickoff` application.

Use this module for production composition and production-scoped configuration. Server-specific runtime settings belong in child server modules.

Production configuration must preserve governance, security, audit, diagnostics, and explicit override boundaries.
