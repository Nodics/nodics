# kickoffLocalCronServer

`kickoffLocalCronServer` is the local cron server module for the `kickoff` application.

Use this module for local scheduled-job server activation, node composition, and runtime topology. Cron capability behavior belongs in reusable cron modules.

Local cron settings should remain layered, observable, and safe to override.
