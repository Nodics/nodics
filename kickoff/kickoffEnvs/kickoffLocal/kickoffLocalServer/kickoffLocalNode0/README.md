# kickoffLocalNode0

`kickoffLocalNode0` is a local runtime node under `kickoffLocalServer`.

Use this node for node-scoped local configuration and activation. Server-wide topology belongs in `kickoffLocalServer`, and capability behavior belongs in active modules.

Node settings should remain explicit, observable, and safe to rebalance across sibling nodes.
