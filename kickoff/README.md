# kickoff

`kickoff` is a project application layer. It composes application modules and environment topology above the reusable Nodics framework.

Use this root for application composition, sample defaults, and project-level configuration. Framework capabilities should be extended through active modules rather than edited in `gFramework`.

Keep kickoff behavior layered, test-backed, and clearly separated from out-of-the-box framework contracts.
