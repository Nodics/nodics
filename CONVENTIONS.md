# Nodics AI Coding Conventions

Follow the canonical Nodics AI agent contract in `AGENTS.md`.

When using tools that load convention files, treat this file as a bridge to the
root and nearest module-level `AGENTS.md` files. The source of truth remains
`AGENTS.md` plus module-local guidance.
