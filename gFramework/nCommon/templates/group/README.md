# Group Template

This directory is the scaffold template for a Nodics group layer. It provides the baseline layout for grouping related modules while preserving Nodics layered override rules.

Keep this template generic and source-of-truth friendly. It should show the expected configuration/package shape without encoding project-specific capabilities.

When group-level module contracts or documentation expectations change, update this template so generated groups begin with the correct structure.
