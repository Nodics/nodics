/**
 * @module nTooling/config/properties
 * @description Reserved non-runtime tooling properties contribution retained for structural consistency across all Nodics modules.
 * @layer config
 * @owner nTooling
 * @override Project tooling behavior belongs in `config/tooling.js`; this runtime property contribution remains empty because nTooling is not application-loadable.
 */
module.exports = {};
