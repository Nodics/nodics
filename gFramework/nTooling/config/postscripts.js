/**
 * @module nTooling/config/postscripts
 * @description Reserved post-script contribution required by the module structure contract; nTooling does not execute during runtime startup.
 * @layer config
 * @owner nTooling
 * @override Tooling commands must not use runtime post-scripts as a parallel command mechanism.
 */
module.exports = {};
