# deapServer

`deapServer` is the development DEAP server module for the `startio` application.

Use this module for dev-scoped data pipeline server activation and runtime topology. Data consumer, processor, and publisher behavior belongs in DEAP modules.

Server configuration should remain layered and environment-scoped.
