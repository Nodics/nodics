# startioDev

`startioDev` is the development environment module for the `startio` application.

Use this module for development-only configuration and server composition. Shared application behavior belongs in `modules`, and server-specific runtime settings belong in child server modules.

Development defaults should stay overrideable and must not become framework defaults.
