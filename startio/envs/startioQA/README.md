# startioQA

`startioQA` is the quality-assurance environment module for the `startio` application.

Use this module for QA configuration and server composition. Server-specific runtime settings belong in child server modules.

QA changes should support validation and test coverage without drifting into framework or production defaults.
