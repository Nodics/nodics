# monoNode1

`monoNode1` is a local runtime node under `monoServer`.

Use this node for node-scoped local configuration and activation. Server-wide topology belongs in `monoServer`, and capability behavior belongs in active modules.

Node settings should remain explicit, observable, and safe to rebalance across sibling nodes.
