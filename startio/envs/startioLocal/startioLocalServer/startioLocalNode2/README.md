# startioLocalNode2

`startioLocalNode2` is a local runtime node under `startioLocalServer`.

Use this node for node-scoped local configuration and activation. Server-wide topology belongs in `startioLocalServer`, and capability behavior belongs in active modules.

Node settings should remain explicit, observable, and safe to rebalance across sibling nodes.
