# backofficeServer docs

This module owns only local BackOffice server composition. Registry schemas,
lifecycle rules, routes, authorization semantics, and self-registration contracts
remain authoritative in `gExp/backoffice`.
