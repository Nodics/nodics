# profileServer

`profileServer` is the local profile server module for the `startio` application.

Use this module for local profile server activation and runtime topology. Profile capability behavior belongs in the active profile modules.

Local profile settings should preserve privacy, access control, tenant context, and layered overrides.
