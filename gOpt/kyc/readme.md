# kyc

`kyc` groups optional know-your-customer capabilities and their API, core, and
schema modules.

Projects can replace providers, workflows, validation, schemas, and routes in
later-index modules without modifying the group or framework core.
