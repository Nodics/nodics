# gOpt

`gOpt` is the optional-capability group. It composes optional business modules
without making them mandatory dependencies of the framework core.

Projects activate or replace optional capabilities through module configuration
and dependency metadata.
