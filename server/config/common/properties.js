module.exports = {
    activeModules: {
        updateGroups: false,
        groups: ['core'], // Group 'framework' will be included automatically
        modules: [
            'server'
        ]
    }
};