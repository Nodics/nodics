# Examples

Use the mock adapter for deterministic approval, rejection, pending, timeout, malformed-response, and webhook-replay tests. A customer adapter is registered by service name through a later module; it must not replace the registry or KYC lifecycle.
