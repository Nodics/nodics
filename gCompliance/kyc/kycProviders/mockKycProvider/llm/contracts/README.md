# Mock contract

The mock implements the full provider operation list and emits only normalized safe evidence. `TIMEOUT` rejects with `KYC_PROVIDER_TIMEOUT`; `MALFORMED` rejects with `KYC_PROVIDER_RESPONSE_INVALID`.
