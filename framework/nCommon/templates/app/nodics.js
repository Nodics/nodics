const nodics = require('../Nodics');

module.exports = (function() {
    nodics.startNodics({});
})();

module.exports = {
    init: function() {
        //put Application specifically code here
    }
};