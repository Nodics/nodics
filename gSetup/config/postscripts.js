/*
    Nodics - Enterprise Micro-Services Management Framework

    gSetup is a non-runtime setup package. This file exists to preserve the
    standard Nodics module folder shape and is not loaded during normal startup.
 */

/**
 * @module gSetup/config/postscripts
 * @description Defines default gSetup configuration used during module startup and layering.
 * @layer config
 * @owner gSetup
 * @override Project, environment, server, node, tenant, or customer layers may override these defaults through Nodics configuration layering.
 */
module.exports = {

};
