- Update updated timestamp for each item update
- Handle Dynamic property handler - Dynamic Attributs can be handled via Mongoose virtuals, Like:
    personSchema.virtual('fullName').get(function () {
      return this.name.first + ' ' + this.name.last;
    });
- Add hook for each layer and each method level
- Start all Active cronJobs on start-up
- Test cronJob update/remove while its running, if it is working fine
- make custom code seperate from Nodics Home directory
- Only active jobs need to be executed
- Define namespace capability - load cleasses from sub-folders
- Add Service to return sample request object for all the services.
- Implement Jsdoc NPM to generate JavaScript Docs
- Make all framework load Prmoisify (Loading all modules in sequence)
- 

- Invalidation process for authToken (Should be common for module level caching)
- Cache Auth token in each module
- Make item and API TTL at configuration level
- Connect Test DB only if required (Check may be its working like this only) - Not possible, If I need to run 
  test cases from API will not work. - Done
- Build common event Listener for all module (May be for cache invalidation)
- Authentication and Authorization for background processes like CronJob Execution
- Create default data import process
- Execute Test cases from API - Done

- Data Import process
- Data Export process

- Client for ActiveMQ
- Client for Kafka