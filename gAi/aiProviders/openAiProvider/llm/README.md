# OpenAI Provider AI Guidance

Read the parent provider contracts before changing this implemented but
disabled adapter. Preserve Responses API-only translation, lifecycle
registration, normalized usage/streaming, opaque credentials, and the shared
conformance suite. Never expose this service directly to a caller.
