# OpenAI Provider Example

Activate this module, configure `aiProviders.providers.openAi.secretReference`,
revisioned pricing, and select `openAi` through a usage profile. Call only
`DefaultAiProviderGatewayService.execute`; never call the adapter service.
