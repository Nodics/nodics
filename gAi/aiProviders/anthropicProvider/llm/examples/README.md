# Anthropic Provider Example

Activate this module, configure
`aiProviders.providers.anthropic.secretReference`, revisioned pricing, and
select `anthropic` through a usage profile. Call only the parent gateway.
