# Anthropic Provider Status

The adapter, lifecycle registration, request and tool translation, streaming
deltas, usage normalization, cancellation, and mocked conformance tests are
implemented. Credentials, selection, retries, diagnostics, and accounting
remain parent `aiProviders` responsibilities.
