# Gemini Provider Example

Activate this module, choose `GEMINI_API` or `VERTEX`, configure only an opaque
secret reference and revisioned pricing, then select `gemini` through a usage
profile. Call only the parent gateway.
