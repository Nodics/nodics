# Gemini Provider Status

The Gemini API and Vertex AI adapter, lifecycle registration, generation,
embedding, function-call translation, streaming deltas, usage normalization,
cancellation, and mocked conformance tests are implemented. Credentials,
selection, retries, diagnostics, and accounting remain parent `aiProviders`
responsibilities.
