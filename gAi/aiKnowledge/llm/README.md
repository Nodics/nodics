# Knowledge AI Guidance

Read `../../AGENTS.md`, this module's `AGENTS.md`, maintained contracts and
examples, then generated context before changing this boundary.

The implemented surface is limited to version 1 API contracts, layered defaults,
and configuration validation/snapshots. Do not describe ingestion, embedding,
indexing, retrieval routes, or synchronization as implemented.
