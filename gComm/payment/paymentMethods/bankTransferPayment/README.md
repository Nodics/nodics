# bankTransferPayment Module

`bankTransferPayment` contributes the Bank Transfer payment-method family. It
supports offline/manual transfer planning and can later be paired with provider
adapters for open-banking or bank-transfer processors.

Payment Core still owns transaction evidence, reconciliation, and lifecycle
transitions.
