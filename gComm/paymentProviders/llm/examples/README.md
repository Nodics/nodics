# Payment Provider Examples

Use the built-in Stripe, PayPal, CyberSource, and Visa provider modules as
mocked/sandbox contract examples. A production customer module should replace
the adapter transport and provider policy while preserving the same normalized
operation methods and Payment-owned transaction lifecycle.
