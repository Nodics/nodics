# Extend Store Classification

In a later-loaded project module, extend the effective `store.store.types` or `store.warehouseAssignment.purposes` arrays through `config/properties.js`. Add project tests proving the effective classification is accepted.

Do not copy the Store schemas, create a second Store registry, or copy Warehouse data into Store. Replace a foundation service only when configuration cannot express the rule, and preserve enterprise isolation and target validation.
